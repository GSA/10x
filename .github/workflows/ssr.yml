name: SSR Build
on: "push"
jobs:
  ssr-build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [12.x]
    steps:
      - name: SSR Build
        run: |
          npm i
          npm run build
